$(document).ready(function() { 
 
  Morris.Bar({
  element: 'graph1',
    data: gon.term_1,
    xkey: 'keyword',
  ykeys: ['wdf_idf'],
  labels: ['WDF*IDF']
});
  Morris.Bar({
  element: 'graph2',
    data: gon.term_2,
    xkey: 'keyword',
  ykeys: ['wdf_idf'],
  labels: ['WDF*IDF']
});
  Morris.Bar({
  element: 'graph3',
    data: gon.term_3,
    xkey: 'keyword',
  ykeys: ['wdf_idf'],
  labels: ['WDF*IDF']
});
  Morris.Bar({
  element: 'graph4',
    data: gon.term_4,
    xkey: 'keyword',
  ykeys: ['wdf_idf'],
  labels: ['WDF*IDF']
});
  Morris.Bar({
  element: 'graph5',
    data: gon.term_5,
    xkey: 'keyword',
  ykeys: ['wdf_idf'],
  labels: ['WDF*IDF']
});
  Morris.Bar({
  element: 'graph6',
    data: gon.term_6,
    xkey: 'keyword',
  ykeys: ['wdf_idf'],
  labels: ['WDF*IDF']
});
  Morris.Bar({
  element: 'graph7',
    data: gon.term_7,
    xkey: 'keyword',
  ykeys: ['wdf_idf'],
  labels: ['WDF*IDF']
});
  Morris.Bar({
  element: 'graph8',
    data: gon.term_8,
    xkey: 'keyword',
  ykeys: ['wdf_idf'],
  labels: ['WDF*IDF']
});
  Morris.Bar({
  element: 'graph9',
    data: gon.term_9,
    xkey: 'keyword',
  ykeys: ['wdf_idf'],
  labels: ['WDF*IDF']
});
  Morris.Bar({
  element: 'graph10',
    data: gon.term_10,
    xkey: 'keyword',
  ykeys: ['wdf_idf'],
  labels: ['WDF*IDF']
});
  Morris.Bar({
  element: 'graph11',
    data: gon.term_11,
    xkey: 'keyword',
  ykeys: ['wdf_idf'],
  labels: ['WDF*IDF']
});
  Morris.Bar({
  element: 'graph12',
    data: gon.term_12,
    xkey: 'keyword',
  ykeys: ['wdf_idf'],
  labels: ['WDF*IDF']
});
})